//Shared Recharts styling and algorithm keys used by line/scatter charts.
export const ALGO_KEYS = ['sequential', 'concurrent', 'parallel']

export const chartGrid = { stroke: '#b8c9db', strokeDasharray: '3 3' }

export const chartTick = {
  fill: '#000000',
  fontSize: 11,
  fontFamily: 'JetBrains Mono',
}

export const chartTooltipStyle = {
  background: '#f0f6fc',
  border: '1px solid #b8c9db',
  borderRadius: 0,
  fontFamily: 'JetBrains Mono',
  fontSize: 12,
  color: '#000000',
}

export const chartLegendStyle = { fontFamily: 'JetBrains Mono', fontSize: 11 }
